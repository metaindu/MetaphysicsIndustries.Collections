using System;
using System.Collections.Generic;
using System.Text;

namespace MetaphysicsIndustries.Collections
{
    //class ExtractCollection<T, U> : ICollection<U>
    //    where U : T
    //{
    //    INotifyCollection<T> _parentCollection;
    //    public ExtractCollection(INotifyCollection<T> parentCollection)
    //    {
    //        if (parentCollection == null) { throw new ArgumentNullException("parentCollection"); }

    //        _parentCollection = parentCollection;
    //        parentCollection.ItemAdded += new CollectionNotification<T>(parentCollection_ItemAdded);
    //        parentCollection.ItemRemoved += new CollectionNotification<T>(parentCollection_ItemRemoved);
    //    }

    //    void parentCollection_ItemRemoved(INotifyCollection<T> collection, T item)
    //    {
    //        if (item is 
    //    }

    //    void parentCollection_ItemAdded(INotifyCollection<T> collection, T item)
    //    {
    //        throw new Exception("The method or operation is not implemented.");
    //    }


    //}
}
