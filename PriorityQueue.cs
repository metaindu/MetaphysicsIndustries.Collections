using System;
using System.Collections.Generic;
using System.Text;

namespace MetaphysicsIndustries.Collections
{
    /*public*/ class PriorityQueue<T, U>
        //where U : ICom
    {
        public PriorityQueue()
        {
        }

        public PriorityQueue(IEnumerable<T> initialContents)
        {
        }

        public PriorityQueue(T[] initialContents)
        {
        }

        
    }
}
